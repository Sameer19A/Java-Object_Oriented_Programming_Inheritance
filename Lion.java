public class Lion extends Animal{   // 'extends' implies that all of the code in the Animal class, exists in the Lion Class

	public Lion(int numTeeth, boolean spots, int weight, double PawSize) {   //constructor called Lion for the class Lion.
		super(numTeeth, spots, weight, PawSize);   //superclass object constructed using the Animal (superclass) parameters.
	
	} // end of constructor Lion 


}